# KoinX Tax Harvesting

React + TypeScript tax-loss harvesting UI.

## Setup

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`

## Features

- Mock API for holdings and capital gains
- Responsive tax harvesting dashboard
- Select holdings to update after-harvesting projections
- Mobile-friendly layout
